#Computing the volume of the sphere
from math import pi       #importing phi from the math library
diam = input('write the diameter of the sphere:")
             #considering the diameter as a variable number
r= float(d)/2.0
volume = (4*pi*((r)**(3)))
print (volume)

